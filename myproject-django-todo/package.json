{
  "name": "django-todo-vercel",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Build complete'",
    "postinstall": "python3 -m pip install -r requirements.txt || pip3 install -r requirements.txt || echo 'Python packages installation attempted'"
  },
  "devDependencies": {}
}